# Online Judge System

Полнофункциональная платформа для спортивного программирования с поддержкой выполнения кода, системой обсуждений и социальными функциями.

## 🏗 Архитектура

Проект состоит из следующих компонентов, запускаемых через Docker Compose:

*   **Frontend**: React (Vite) приложение с современным UI (Tailwind CSS, Radix UI). Доступно на порту `80` (через Nginx).
*   **Backend**: NestJS API. Доступно на порту `3001`.
*   **Execution Engine**: Judge0 (запущен в VirtualBox VM или Docker, в зависимости от конфигурации), порт `2358`.
*   **Database**: PostgreSQL для хранения данных пользователей, задач и обсуждений. Порт `5432`.
*   **Cache/Queue**: Redis для очередей задач (BullMQ) и кэширования. Порт `6379`.
*   **Proxy**: Nginx раздает статику фронтенда и проксирует запросы к API.

## 📂 Структура проекта

```
project-root/
├── nexus-frontend/       # Исходный код React приложения
│   ├── src/              # Компоненты, страницы, хуки
│   └── Dockerfile        # Dockerfile для сборки фронтенда
├── src/                  # Исходный код NestJS бэкенда
│   ├── problems/         # Модуль задач
│   ├── submissions/      # Модуль отправки решений
│   ├── judge/            # Интеграция с Judge0
│   └── ...
├── tests/                # Скрипты тестирования и утилиты
│   ├── full-system-check.js  # Проверка работоспособности системы
│   ├── create-problem.js     # Скрипт создания тестовых задач
│   └── ...
├── docker-compose.yml    # Конфигурация всех сервисов
└── start.sh              # Скрипт быстрого запуска
```

## 🚀 Быстрый старт

### Требования
*   Docker & Docker Compose
*   Node.js 18+ (для запуска локальных скриптов)

### Запуск

1.  **Клонировать репозиторий:**
    ```bash
    git clone <repository-url>
    cd online-judge
    ```

2.  **Запустить все сервисы:**
    ```bash
    docker-compose up -d --build
    ```

3.  **Открыть в браузере:**
    Перейдите по адресу: [http://localhost](http://localhost)

## 🔑 Учетные данные

**Web приложение:**
*   **Admin**: Login: `admin`, Password: `admin123`
*   **User** (создается скриптом): Login: `user1`, Password: `password123`
*   *Регистрация новых пользователей также открыта.*

**Сервисы:**
*   **PostgreSQL**: `postgres` / `M_asdf_321` (Port 5432)
*   **Judge0**: [http://localhost:2358](http://localhost:2358)

## 🌟 Функционал

### Для пользователей:
*   **Решение задач**: Поддержка Python, C++, JavaScript. Monaco Editor с подсветкой синтаксиса.
*   **Выполнение кода**: Реальное выполнение кода с ограничениями по времени и памяти.
*   **Обсуждения**: Возможность обсуждать задачи, голосовать за ответы.
*   **Top Solutions**: Просмотр лучших решений после успешного выполнения задачи.
*   **Темная тема**: Полная поддержка Dark Mode.
*   **Профиль**: История решений, избранные и лайкнутые задачи.

### Для администраторов:
*   **Управление задачами**: Создание новых задач с тестами и ограничениями.
*   **Модерация**: Возможность отмечать "Лучшие решения" и "Ответы" в обсуждениях.

## 🛠 Полезные команды

**Проверка статуса системы:**
```bash
docker ps
# или
node tests/full-system-check.js
```

**Перезапуск бэкенда (например, после изменения кода):**
```bash
docker-compose restart backend
```

**Пересборка фронтенда:**
```bash
docker-compose build frontend --no-cache
docker-compose up -d frontend
```

**Просмотр логов:**
```bash
docker-compose logs -f backend
```

## 🧪 Тестирование

В папке `tests/` находятся полезные скрипты:
*   `full-system-check.js`: Проверяет доступность всех контейнеров и API.
*   `test-api-login.js`: Проверяет процесс логина через API.
*   `test-create-problem.js`: Создает тестовую задачу через API (требует прав админа).

---
*Developed by Ferumm Team*
